{
  "name": "kestrel",
  "version": "1.0.0",
  "scripts": {
    "start": "python run_app.py",
    "heroku-postbuild": "cd kestrel-frontend && npm install && npm run build"
  },
  "engines": {
    "node": "14.x"
  }
}
