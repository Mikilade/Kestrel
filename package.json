{
    "name": "kestrel",
    "version": "1.0.0",
    "scripts": {
      "start": "cd app && flask run",
      "heroku-postbuild": "cd kestrel-frontend && npm install && npm run build"
    },
    "engines": {
      "node": "14.x"
    }
  }